# Copy to archive_server/.env (gitignored) and fill in real values.
# Load with `export $(cat archive_server/.env | xargs)` or a process manager that reads .env files.

DATABASE_URL=postgresql://yacaid:REPLACE_ME@localhost:5432/yacaid_archive
MEDIA_ROOT=/var/lib/yacaid/media

# Shared secret the recorder's archive_sync client authenticates with (Authorization: Bearer ...)
ARCHIVE_API_KEY=REPLACE_WITH_RANDOM_LONG_STRING

# Used to sign UI session cookies — keep this secret and stable across restarts
SESSION_SECRET=REPLACE_WITH_RANDOM_LONG_STRING

# Recorder's local control API, reachable only over a private tunnel (Tailscale/WireGuard)
CONTROL_BASE_URL=http://100.x.x.x:8090
CONTROL_API_TOKEN=REPLACE_WITH_SAME_TOKEN_AS_RECORDER_CFG

# Set to "false" only for local HTTP testing — must be "true" in production (HTTPS)
COOKIE_SECURE=true
